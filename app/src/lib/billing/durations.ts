/** 1 mês — plano de entrada. */
export const MONTH_DURATION_DAYS = 30;

/** 3 meses — plano base (preço de referência). */
export const QUARTER_DURATION_DAYS = 91;

/** ~6 meses — alinhado ao calendário acadêmico (`SCOPE-CLOUD` §3.1). */
export const SEMESTER_DURATION_DAYS = 182;

/** 12 meses — plano anual com desconto vs. 2 semestres. */
export const YEAR_DURATION_DAYS = 365;

/** 5 anos — plano longo para graduação inteira. */
export const FIVE_YEAR_DURATION_DAYS = 365 * 5;
